# iRead AI 작업 지침

이 저장소는 제품 컨텍스트, 서비스 경계, 계약과 주요 결정을 관리하는 오케스트레이션 저장소다. 서비스 구현은 `services/*` submodule에서 수행한다.

## 작업 시작

1. `docs/index.md`에서 요청과 직접 관련된 기준 문서를 찾는다.
2. 현재 사실이 필요하면 `docs/context/project-context.md`를 읽는다.
3. 중요한 결정이나 장기 작업이면 관련 ADR과 실행 계획을 확인한다.

모든 문서를 미리 읽거나 프로젝트 사실을 이 파일에서 다시 관리하지 않는다.

## 작업 전 확인

- 읽기 전용 탐색으로 확인할 수 있는 사실은 먼저 확인한다.
- 제품 범위, 사용자 데이터, 서비스 경계, API 계약, 새 의존성, 외부 시스템, 보안·권한 또는 파괴적 작업에 결과를 바꾸는 선택이 남으면 변경 전에 사용자에게 질문한다.
- 작은 구현 세부사항은 기존 코드와 기준 문서에 맞춰 합리적으로 결정할 수 있다.
- 가정이 결과에 영향을 주면 `[ASSUMPTION]`, 미결 사항은 `[TBD]`, 외부 결정을 기다리면 `[BLOCKED]`로 표시한다.

## 변경 원칙

- 가장 가까운 기준 원본을 먼저 수정하고 실제로 영향을 받은 문서만 동기화한다.
- API·데이터베이스·기능 계약은 `docs/workflows/specification-management.md`를 따른다.
- 되돌리기 어렵거나 여러 팀에 영향을 주는 결정은 ADR로 기록한다.
- 문서 추가·삭제·이름 변경 때만 관련 인덱스를 갱신한다.
- 상세 문서 형식은 `docs/workflows/documentation-style.md`를 따른다.

## 계획

여러 세션이 예상되거나 둘 이상의 서비스 계약, 마이그레이션, 보안·배포처럼 복구 비용이 큰 작업에만 `PLANS.md`에 따라 실행 계획을 만든다. 한 세션의 작은 변경은 대화 내 체크리스트로 충분하다.

## 검증

- 동작이 바뀌거나 기존 검증에 공백이 생기면 테스트를 추가하거나 수정한다.
- 기존 테스트가 변경 동작을 충분히 검증하면 관련 테스트 실행만으로 충분하다.
- 공통 기반이나 넓은 범위에 영향을 주면 해당 서비스 전체 테스트를 실행한다.
- 문서·하네스 변경은 `python tools/validate_harness.py`를 실행한다.
- 계약 변경은 관련 계약 검증을 추가로 실행한다.
- 중요한 검증이 불가능하면 차단 사유와 대체 검증을 보고한다.
- 필수 검증이 성공하기 전에는 작업이나 백로그를 `done`으로 표시하지 않는다.

## Git

Git 작업은 `docs/workflows/git-flow.md`를 따른다. `main` 직접 push, 공유 브랜치 force push와 이력 재작성은 금지한다.

## 완료 보고

결과, 변경한 범위, 실행한 검증과 남은 중요한 위험을 간단히 보고한다. 현재 변경과 무관한 검증을 나열하지 않는다.
